[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gatesmith-core"
version = "0.1.0"
description = "Deterministic Boolean to NAND/TapeOut byte compiler spike"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["gatesmith_core"]
